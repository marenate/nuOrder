import { Selector } from 'testcafe';
export default class PageLogin{ constructor(){
    this.selectors = {
        postTitle: Selector('p.site-title'),
        postDescription: Selector('p.site-description'),
        postTitle: Selector('h1.entry-title'),
        postTitle: Selector('a'),
        
    }
}
}